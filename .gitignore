# binaire compilé
alvalllm
*.o

# artefacts Python
__pycache__/
*.pyc

# notes perso / hors dépôt
CLAUDE.md
memory/

# data/ : seul params.json.example est versionné.
# Le reste (models, collectes, params perso) est généré par les scripts
# (collect_models.py + couplage.py) et/ou local — jamais synchronisé.
data/*
!data/params.json.example
